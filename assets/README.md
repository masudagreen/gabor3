# assets/

アイコン・スプラッシュ・画像素材置き場。

Designer 確定後にここへ：
- icon.png (1024×1024)
- splash.png
- favicon.png
- adaptive-icon.png (Android)

を配置し、app.json の `icon` / `splash.image` / `web.favicon` / `android.adaptiveIcon.foregroundImage` を有効化する。
