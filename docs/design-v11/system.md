# GaborEye v1.1 — デザインシステム

このドキュメントは GaborEye v1.1（13 ゲーム化リブート、Sprint 8〜19）+ v1.1.1（Sprint 20 UX リファイン）+ v1.1.2（Sprint 21 全ゲームのボタン撤去とガボール直接選択統一）のデザインシステム全体を定義します。
v1 デザインシステム（`docs/design/system.md`）を **継承** したうえで、v1.1 で追加・改変・削除した規範のみを差分として明示します。

> **v1.1.1 改訂（Sprint 20、2026-04-30）**：実機ユーザーテストを受けて以下を改訂。詳細は §1.1 注 / §1.3 注 / `docs/design-v11/sprints/sprint-20/screens.md` を参照。
> 1. F-07 選択枠を「黄色 4px」→「控えめな枠（2px 以下、ガボール視認性を阻害しない色）」に
> 2. F-10 結果サマリを独立画面遷移から「刺激画面オーバーレイ統合（◯／✕ + 『次へ』ボタン）」に。**閾値メトリクス表示は ResultOverlay には含めず、F-11 進捗グラフから参照する設計**（実機テストでメトリクスバーが刺激の視認を妨げるとのフィードバックを受けて再簡素化）
> 3. G-02 / G-08 の選択肢をテキスト 2 択ボタンからガボール直接選択に統一
>
> **v1.1.2 改訂（Sprint 21、2026-05-01）**：Sprint 20 の G-02 / G-08 限定刷新を実機テストしたところ、ユーザーから「他のゲームでも回答ボタンが表示されているままで、回答が直感的に対応づかない／回答表示がされない」とのフィードバックを得たため、**全ゲームの選択肢を刺激領域内の直接タップ方式に統一**する設計に再改訂した。直接選択困難なゲーム（G-09 / G-12 / G-13）は Designer 判断で UI 構造を確定。G-10 / G-11 は構造変更で直接選択化。詳細は §1.1 注 / §1.3.1 注 / `docs/design-v11/sprints/sprint-21/screens.md` を参照。
> 1. F-07 回答方式の原則を「全ゲームで刺激領域内のパッチ／象限／要素を直接タップして回答する」に統一（補助テキスト選択肢ボタンは原則撤去）
> 2. G-03 / G-04 / G-05 / G-06 / G-11 はガボール直接選択に統一（horizontal-2 / clock-8 撤去）
> 3. G-10 は 8×8 grid を 4 象限クリッカブル化（grid-4 ボタン撤去、案 A 採用）
> 4. G-09（horizontal-2 維持）/ G-12（grid-4 維持）/ G-13（keypad-10 維持）は直接選択困難なため Designer 判断でボタン UI を残し、ボタンを刺激領域直下に空間対応配置（案 B 採用）
> 5. F-10 結果オーバーレイの「ユーザー操作対象に ◯/✕ を重畳する」UX を補強。直接選択ゲームではガボール／象限上、ボタン維持ゲームでは当該ボタン上に重畳
>
> 永続化スキーマ・staircase 値・閾値計算ロジック・採点ロジックは不変。

> **位置づけ**：v1.1 はクリーンルーム再設計（A-2、F-17）。コードは新規だが、デザイントークンは v1 の成熟したものを最大限再利用します。
> 本書を読むときは、まず v1 `docs/design/system.md` の §1〜§17 を読み、そのうえで本書の §1.1 / §1.2 / §1.3（v1.1 拡張・改訂・削除）を上書きしてください。

---

## 0. v1 → v1.1 の変更サマリ

| 領域 | v1 | v1.1 | 関連仕様 |
|---|---|---|---|
| ゲーム数 | 3（Game 1〜3） | **13（G-01〜G-13）** | F-07、§7 |
| 共通フォーマット | ゲームごとにバラバラ（マスク・短時間提示など） | **OPT-12 全 13 ゲームで「60 秒注視・画像切替なし・自由回答変更可・自動採点」に統一** | §1.4（本書） |
| 距離リマインド | 「準備ができました」ボタンで進行 | **3 秒カウントダウン → 自動進行**（OPT-10 改訂、F-16） | §1.5（本書） |
| 標準フロー | 3 ゲーム連続コース（約 3 分） | **13 ゲーム連続コース（約 13 分）** | F-04、F-05 |
| ホーム CTA | 「3 分コースを始める」（既存） | **「全ゲーム連続プレイ（約 13 分）」** | F-04 |
| 結果サマリ | ゲームごとに別構成 | **v1.1：「正解 + あなたの回答 + 今回の閾値 + 前回比」を 1 枚で統一 → v1.1.1：刺激画面に ◯/✕ + 『次へ』ボタンのみを重畳（閾値メトリクスは F-11 進捗グラフ側で参照）** | F-10 |
| 進捗グラフ | V1 スコア折れ線（単一） | **タブ切替（ワイドスコア / ゲーム別 13 タブ）** | F-11 |
| バッジ | 8 種 | **13 種（B-01〜B-13）** | F-13、§10 |
| データ | v1 永続化 | **起動時に v1 由来データを全消去 → v1.1 用キーで初期化**、初回 1 度通知 | F-17、§11 |
| リリース選定 | 全機能リリース | **`gameRegistry.releaseEnabled` フラグで動的除外**（ホーム / コース / グラフ / バッジ） | F-18 |
| `GaborWithMask` | Game 2 で使用 | **廃止**（OPT-12 でマスク提示が消えたため） | §1.6（本書） |
| `font.caption`（20px） | 一部使用 | **使用さらに限定**（v1.1 の主要動線は全て 24px 以上） | §1.7（本書） |
| `motion.duration.gameMaskInterval` | 200ms | **未使用**（ガボール提示は 60 秒固定、マスクなし） | §1.6（本書） |

> 上記変更点以外は v1 デザインシステムのトークン値・コントラスト規範・タイポ・スペース・モーション・i18n・コピーライティング規範をすべてそのまま継承します。

---

## 1. v1.1 で新規・改訂・削除する設計規範

### 1.1 OPT-12 全ゲーム共通注視フォーマット（新規・最重要）

13 ゲーム全てが以下の画面骨格に従います。これは v1 デザインシステムの §0「老眼最適化原則」に追加される **OPT-12** に相当する規範です。

```
┌──────────────────────────────────────────────┐
│  ✕    残り N 秒                              │ ← GameStatusBar (v1.1)
│                                              │   左：閉じる × (48pt)
│                                              │   中：font.h2 30px Bold tabular-nums
│                                              │   右：何も置かない（試行数表記廃止）
│  ┌────────────────────────────────────────┐  │
│  │                                        │  │
│  │      [ ゲーム固有の注視領域 ]           │  │ ← 60 秒間ずっと表示
│  │      （点滅・切替・マスクなし）         │  │   gabor.frame.padding=24
│  │                                        │  │   背景は #808080 固定
│  │                                        │  │
│  └────────────────────────────────────────┘  │
│                                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐    │ ← 選択肢ボタン群
│  │ 選択肢 A  │  │ 選択肢 B  │  │ 選択肢 C  │    │   AnswerChoiceGroup
│  │ (ハイライト│  │           │  │           │    │   選択中＝黄色 4px 枠
│  │  4px 黄)  │  │           │  │           │    │   タップで切替
│  └──────────┘  └──────────┘  └──────────┘    │   再タップで解除
│                                              │
└──────────────────────────────────────────────┘
```

#### 共通骨格の遵守規範
- **画面上部固定領域（高 64px）**：左に × ボタン（IconButton lg、ariaLabel="ゲームを中断"）、中央に「残り N 秒」（`font.h2` 30px Bold tabular-nums）。**v1 で右側に表示していた「N / M 試行」は廃止**（OPT-12：1 セッション 1 試行になったため）
- **中央注視領域**：ゲーム固有のガボール／グリッド／パッチを 60 秒間「点滅なし・切替なし・フェードなし・マスクなし・モーフィング許容（G-01 のみ）」で表示
- **下部選択肢群**：`AnswerChoiceGroup`（v1.1 新規コンポーネント、`components.md` §AC-1 参照）。タップで選択トグル、別を押すと切替、再タップで解除
- **「現在の回答：◯◯」のテキスト表示は置かない**（Q5 確定）。選択中はボタン／画像自体が枠でハイライトされる
- **試行中の正誤フィードバックは出さない**（注視を妨げない）。F-10 結果オーバーレイで開示

> **v1.1.1 改訂（Sprint 20）**：v1.1 では「黄色 4px 枠」をハイライトとしていたが、実機テストで「黄色 4px 枠が目立ちすぎてガボールが見えづらい」フィードバックを受け、**控えめな選択枠（2px 以下、ガボール視認性を阻害しない中性色）** に変更した。具体値は `components.md` §4 `ImageChoiceCell` および §3 `AnswerChoiceGroup` で定義する。新トークン `color.selection.subtle` を導入（後述）。

> **v1.1.2 改訂（Sprint 21）**：選択肢領域は「**刺激領域内の要素（ガボール／象限）を直接タップ**」を原則とし、上記 OPT-12 の骨格における `下部選択肢ボタン群` 領域は **G-09 / G-12 / G-13 でのみ表示** され、それ以外のゲームでは選択肢ボタンは存在しない（パッチ自体が ImageChoiceCell でラップされて選択肢になる）。詳細は `sprints/sprint-21/screens.md` 参照。直接選択原則化に伴い「ガボール領域は装飾扱いで `aria-hidden="true"`」の規約は撤回し、ImageChoiceCell でラップされたガボールは radio として SR にも到達可能にする（具体は components.md §4 a11y 節）。

- **未回答のまま 60 秒経過は「不正解」**として記録、staircase は易方向に動く
- **早期終了不可（OPT-11）**。中断は × ボタンのみ（AbortConfirmDialog 経由）

#### 画面分割の数値規範（スマホ 375×667 基準）

| 領域 | 高さ | 幅 |
|---|---|---|
| GameStatusBar | 64px | 100% |
| 注視領域パディング | space.4（16px）天地 | space.4 左右 |
| 注視領域コンテンツ | 280〜320px（ゲームごとに最適化、`min(viewport.width-32, 360)`） | 同左 |
| 選択肢領域パディング | space.5（24px）上、space.6（32px）下 | space.4（16px）左右 |
| 選択肢ボタン | 64px（lg） | flex-1 / 各 minWidth 96px |

#### PC 横 1280×800

中央 720px 最大幅、ガボール領域は最大 480×480px（v1 と同じ）。選択肢は注視領域の下に水平に並べる。注視領域脇に置かない（読み上げ順序の都合）。

#### a11y
- 選択中の選択肢は `aria-checked="true"`（ラジオグループ）または `aria-pressed="true"`（トグル単体）
- カウントダウンは `aria-live="polite"`、5 秒以下 polite、6 秒以上 off に切替（v1 と同じ）
- ガボール領域は装飾扱いで `aria-hidden="true"`、ARIA テキストとしてゲーム固有のガイド（「左右 2 つの縞模様のうち、どちらが時計回りに傾いているかを選んでください」）を `aria-describedby` で読ませる

---

### 1.2 F-04 ホーム画面の構造規範（新規）

v1 の「3 分コースを始める」CTA とは別に、v1.1 のホームは以下を**強制構造**とします。

```
┌─────────────────────────────────────┐
│  GaborEye                       ⚙   │ ← 左ロゴ、右設定 IconButton (lg, 56pt)
│                                     │
│  本日完了 ✓     ストリーク 🔥 23 日 │ ← 本日完了マーク + StreakBadge
│                                     │
│       ┌────────────────────┐        │
│       │                    │        │
│       │  全ゲーム連続プレイ  │        │ ← プライマリ CTA (lg, 64px 高、56pt+)
│       │   （約 13 分）       │        │   ラベル font.body.lg 26px Bold
│       │                    │        │   背景 color.action.primary (#13449D)
│       └────────────────────┘        │
│                                     │
│       単体プレイ（13 ゲームから）  ›   │ ← セカンダリリンク (tertiary, 56pt+)
│                                     │   font.body 24px、下線
│                                     │
│  ─────────────────────────────────  │
│                                     │
│  📊 進捗グラフ      🏅 バッジ       │ ← 2 列ナビゲーション
│  （直近 28 日推移） （13 種達成）    │   それぞれ Card pressable, 80pt 高
│                                     │
└─────────────────────────────────────┘
```

#### 規範
- **プライマリ CTA は画面中央付近**（vertical center ±10%）に配置。「全ゲーム連続プレイ（約 13 分）」**1 個のみ**（OPT-3 主要 CTA は 1 個）
- セカンダリの「単体プレイ」は CTA の直下、tertiary variant で配置
- 「本日完了 ✓」はその日のフルコース完了済みのときのみ表示（DailyStats.fullCourseCompleted）
- StreakBadge は本日完了の有無に関わらず常時表示（数値が 0 のときは「コースを始めて、連続記録をスタート」を表示）
- 設定 IconButton は右上、進捗グラフ・バッジへの入口は画面下部の 2 列ナビとして配置
- **`releaseEnabled=false` のゲーム数の動的反映**：CTA ラベルの「（約 13 分）」は `releaseEnabled=true` のゲーム数 N を元に動的計算し「（約 N 分）」と表示する（13 ゲーム時 13 分、10 ゲーム時 10 分）

#### F-18 動的除外の保証
本書 §3 に「設計上の `releaseEnabled` 反映ルール」を集約します（後述）。

---

### 1.3 F-10 結果サマリの統一構造（v1.1 改訂、最重要 / v1.1.1 で再改訂）

v1 では Game 1/2/3 ごとに結果サマリのレイアウトが異なっていました。v1.1 では **13 ゲーム全部が以下の 1 枚レイアウトに統一** されました。

> **v1.1.1 改訂（Sprint 20）**：v1.1 までは独立した結果画面に遷移していたが、実機テストで「文字だけでは自分が画面のどこを選んだのか視覚的に対応づけられない」フィードバックを受け、**結果開示を刺激画面に統合**する方式に再改訂した。本 §1.3 の枠囲みレイアウトは「v1.1 期の旧レイアウト」として記録目的で残し、v1.1.1 以降の最新規範は **§1.3.1 結果オーバーレイ統合方式** で上書きする（後述）。
> Sprint 20 の詳細は `docs/design-v11/sprints/sprint-20/screens.md` を参照。

```
┌─────────────────────────────────────┐
│              G-04 の結果             │ ← font.h2 30px Bold（ゲーム名）
│                                     │
│         正解は 「右が濃い」          │ ← font.h1 36px Bold + 黄色装飾
│                                     │
│         あなたの回答 「左が濃い」    │ ← font.body.lg 26px Medium
│                                     │   不正解時：error 色装飾アイコン併記
│                                     │
│  ┌───────────────┐ ┌───────────────┐│
│  │ 今回の閾値     │ │ 前回比         ││ ← 2 列メトリクスカード
│  │               │ │               ││   metric-card .value
│  │   0.12       │ │   +0.03 ↑     ││   font.h2 30px Bold
│  │ コントラスト差 │ │  改善         ││   ラベル font.body 24px
│  └───────────────┘ └───────────────┘│
│                                     │
│  ┌───────────────────────────────┐  │
│  │     次へ                      │  │ ← Primary Button (lg, 64px)
│  └───────────────────────────────┘  │   コース時：右上に 10 秒カウントダウン併記
│                                     │   単体時：自動進行なし
└─────────────────────────────────────┘
```

#### 規範
- 「正解は ◯◯」を最上段に大きく表示（`font.h1` 36px Bold + 黄色 4px 枠装飾、点滅なし）
- 「あなたの回答 ◯◯」を 2 段目（`font.body.lg` 26px Medium）。不正解時は文字色は `color.fg.primary`、装飾アイコンのみ `palette.semantic.error`（§1.4 制約に従い、テキスト色には使わない）
- 「今回の閾値」と「前回比」を 2 列カード（`MetricCard` v1.1 新規、`components.md` §MC-1 参照）。値は `font.h2` 30px Bold（数値強調）、ラベルは `font.body` 24px
- 前回比の direction（up/down/flat）は装飾矢印アイコン（success / warning / muted 色）+ テキスト「改善」「同等」「やや低下」で表現。色のみで判別させない（NF-12）
- 単体プレイ時は「次へ」ボタンを押すまで進まない（OPT-7）
- コースモード時は「次へ」ボタンの右上に **10 秒カウントダウン**（`font.h3` 26px、CountdownDisplay md）を併記し、ユーザー操作なしで自動進行
- バッジ獲得時はサマリ画面下部に AchievementBadge 獲得演出（v1 同仕様）を 1.5 秒挿入

#### 比較演出
- 「+0.03 改善」のような前回比は、v1.1 staircase 改訂（直近 5 セッション平均）に従い、前回までの 5 セッション平均 vs 今回値の差分で計算
- データが 1 件目（前回なし）のときは「初回測定」と表示、矢印は表示しない

---

### 1.3.1 結果オーバーレイ統合方式（v1.1.1 新規・最重要、F-10 再改訂、v1.1.2 で全ゲーム直接選択原則を追加）

Sprint 20 で導入。13 ゲーム共通で、60 秒経過後は刺激画面に**とどまったまま**結果を開示する。独立した結果画面（旧 `ResultSummaryV11` の独立画面・旧 `CourseInterstitialResultScreen`）への遷移は廃止。

> **v1.1.1 仕様再改訂（Sprint 20、spec 再確定）**：実機テストの追フィードバックで「結果オーバーレイ上のメトリクスバー（閾値・前回比・単位）が刺激領域の視認を妨げる」との指摘を受け、**メトリクスバーは ResultOverlay から完全撤去**した。閾値の数値推移は **F-11 進捗グラフ**で参照する設計とする。結果オーバーレイの構成は **◯ / ✕ アイコン + 『次へ』ボタン（およびコースモード時のカウントダウン）のみ**となる。

> **v1.1.2 仕様再々改訂（Sprint 21、2026-05-01）**：実機テストで「他のゲームもガボールの下にボタンの選択肢が残っているのが煩わしい／回答表示が直感的でない」とのフィードバックを受け、**全ゲームでガボール／刺激領域を直接タップして回答する形を原則化**する。これに伴い、結果オーバーレイの ◯/✕ 重畳位置も **「ユーザー操作対象（直接選択ゲームならガボール／象限、ボタン維持ゲームならボタン）の中央」に統一**する（v1.1.1 と同方針だが対象範囲が広がる）。直接選択化したゲーム（G-03 / G-04 / G-05 / G-06 / G-10 / G-11）では刺激領域内のガボール／象限上に、Designer 判断でボタン UI を残したゲーム（G-09 / G-12 / G-13）ではボタン上に ◯/✕ を重畳する。

```
┌─────────────────────────────────────┐
│  ✕    残り 0 秒（停止）              │ ← GameStatusBarV11 そのまま
│                                     │   ✕ は維持（ホーム強制脱出用）
│  ┌────────────────────────────────┐ │
│  │                                │ │
│  │   ┌──────┐    ┌──────┐         │ │ ← 注視領域はそのまま残す
│  │   │ ▦/▦  │    │ ▦\▦  │         │ │   GE-01〜GE-13 の刺激は固定
│  │   │  ◯   │    │  ✕   │         │ │   各パッチ／領域の中央に
│  │   └──────┘    └──────┘         │ │   ◯ / ✕ アイコンを重畳表示
│  │                                │ │   （半透明背景 + アイコン）
│  └────────────────────────────────┘ │
│                                     │
│  ┌─────────────────────────────────┐│
│  │     次へ          ⏱ 8           ││ ← Primary lg 64px
│  └─────────────────────────────────┘│   コース時のみ右側にカウントダウン併記
└─────────────────────────────────────┘
```

#### 規範

1. **画面遷移しない**：60 秒経過後、`GamePlaySurface` の `stimulusArea` はそのまま固定表示し、`answerChoices` 領域のみが「『次へ』ボタン」に置き換わる。`stimulusArea` 内の各選択対象（パッチ／グリッドセル／領域）の上に **◯ / ✕ アイコンレイヤー**を重畳する
2. **◯ アイコン**：採点における「正解」位置（出題者が指定した正解側）に表示。色は `palette.semantic.success`（緑系）、半透明白背景の上に配置
3. **✕ アイコン**：「ユーザーが選択したが不正解だった」位置のみに表示。色は `palette.semantic.error`（赤系）、半透明白背景の上に配置
4. **正解と一致して選んだ位置**：◯ のみ表示（✕ は付かない）。✕ + ◯ の同時重畳は発生しない
5. **複数選択ゲーム（G-01 / G-07 / G-09）**：各パッチ単位で個別に ◯ / ✕ を判定して重畳。「正解だが選ばれなかった」パッチには ◯ を表示するが、不透明度を 50% 落として「取りこぼし」を視覚的に区別する（薄 ◯）。「不正解で選ばれなかった」パッチには何も表示しない
6. **メトリクスバー（閾値・前回比・単位）は表示しない**：刺激の視認を妨げる追加メトリクスは ResultOverlay には載せない。閾値の数値や時系列推移は F-11 進捗グラフから参照する設計とする
7. **「次へ」ボタン + カウントダウン**：刺激領域の直下に Primary Button lg（64px、`font.body.lg` 26px Bold）を配置。コース時は右内側にカウントダウン（`font.h3` 26px tabular-nums）を併記、10 秒で自動進行。単体時はカウントダウンなしで押すまで停止
8. **試行中（注視 60 秒間）は ◯/✕ オーバーレイを表示しない**（F-07 と矛盾しない）
9. **オーバーレイ表示中は `stimulusArea` 内の選択肢タップは無効**（採点後の誤操作防止）。`pointer-events: none` を `stimulusArea` 全体に適用
10. **アニメ・点滅は使わない**（OPT-9 / NF-11）。フェードイン 200ms（`prefers-reduced-motion: reduce` 時は 0ms）でオーバーレイが現れる
11. **バッジ獲得演出**（F-13、1.5 秒）はオーバーレイの上層レイヤで 1 度だけ流れる（位置は画面中央上、「次へ」ボタンと干渉しない）

#### a11y

- 60 秒経過時に `aria-live="assertive"` で結果文字列を 1 度読み上げ（例：「G-04 結果。正解は右が濃い。あなたの回答は左が濃い。次へボタン」）
- ◯ / ✕ アイコンには `aria-label="正解です" / "不正解です"` を付与
- 「次へ」ボタンに自動フォーカス（カウントダウンは継続）
- 単体時は「次へ」のすぐ下に `SinglePlayPostFooter`（同じゲームをもう一度／一覧へ／ホームへ）が表示されるため、フッター用の縦スクロールを許容する。コース時はフッターを表示しない

#### 旧 §1.3 レイアウトとの関係

- 旧 §1.3（独立画面の `ResultSummaryV11`）は v1.1.1 では撤去。`ResultSummaryV11` が表示していた閾値メトリクスは ResultOverlay には**引き継がれない**（F-11 進捗グラフで参照する設計）
- `MetricCard` も ResultOverlay 内では使用しない（メトリクスバー自体が撤去されたため）
- `SinglePlayPostFooter`（FT-1）は本オーバーレイの「次へ」ボタンの直下に縦に展開される（単体時のみ）

---

### 1.4 F-11 進捗グラフのタブ構造（v1.1 改訂）

v1 では V1ScoreChart 単一 + 日次ベスト切替でしたが、v1.1 は以下のタブ構造です。

```
┌─────────────────────────────────────┐
│  ←  進捗グラフ                      │ ← タイトル
│                                     │
│ ┌─────────────────┬───────────────┐ │
│ │ ワイドスコア     │  ゲーム別      │ │ ← Tab 2 つ（v1.1 改訂）
│ │ （全 13 平均）  │  （13 タブ）   │ │   font.body.lg 26px
│ └─────────────────┴───────────────┘ │   選択中は下線 4px brand.primary
│                                     │
│  [ ワイドスコア折れ線グラフ 28 日 ]   │
│                                     │
│  または「ゲーム別」タブ選択時：       │
│  ┌─G-01─┬─G-02─┬─G-03─┬─...────┐  │ ← 子タブ（横スクロール）
│  │       │       │       │        │  │   releaseEnabled=true のみ表示
│  └───────┴───────┴───────┴────────┘  │
│  [ G-01 の閾値折れ線グラフ 28 日 ]    │
│                                     │
└─────────────────────────────────────┘
```

#### 規範
- 親タブ 2 つ：「ワイドスコア」「ゲーム別」
- 子タブ（ゲーム別の中）：13 ゲームの横スクロール（スマホ）／グリッド（PC）。`releaseEnabled=true` のみ表示し、false は非表示
- ワイドスコアの算出：`releaseEnabled=true` のゲームの正規化スコア平均（spec.md §F-11 参照）
- グラフ本体は v1 V1ScoreChart コンポーネントを継承（軸ラベル `font.body` 24px、当日強調点）
- ゲーム別タブでは Y 軸スケールが各ゲームの閾値範囲に応じて自動調整（パラメータの単位「度」「コントラスト差」「cpd 比」「arcmin」など、ゲームごとに差異あり、ラベル併記）
- データ 7 日未満時は v1 と同じく「もう少しデータが集まると傾向が見えます」を表示

---

### 1.5 F-16 距離リマインドの改訂（OPT-10 改訂）

v1 では「準備ができました」ボタンを押して進行していましたが、**v1.1 では 3 秒カウントダウンで自動進行**（Q11 確定）。

```
┌─────────────────────────────────────┐
│  ✕                                  │ ← 緊急脱出のみ（タップで戻る）
│                                     │
│        画面から 40cm                 │ ← font.h1 36px Bold
│         離れてください                │
│                                     │
│       [ 人と画面のイラスト ]          │ ← distance-40.svg、静止
│                                     │
│       片目を覆ってください（任意）    │ ← oneEyeGuidance ON 時のみ
│                                     │   font.h3 26px
│                                     │
│            ┌────┐                   │
│            │  3  │                  │ ← 3 秒カウントダウン
│            └────┘                   │   font.numeric.xl 72px tabular-nums
│         秒後に自動で開始             │   font.body 24px
│                                     │
└─────────────────────────────────────┘
```

#### 規範
- 「準備ができました」ボタンは**廃止**（v1 から削除）
- 3 秒カウントダウンを画面中央〜下に大きく表示（`font.numeric.xl` 72px tabular-nums）
- カウント 0 で自動的にゲーム画面に遷移（`motion.duration.base` 200ms フェード）
- 緊急脱出 × ボタンのみ表示。距離変更リンクも v1.1 では廃止（設定画面で変更）。これは「3 秒で自動進行」の意図を尊重するため
- カウントダウン中は `aria-live="polite"` で 1 秒ごとに「3 秒後に開始」「2 秒」「1 秒」と読み上げ。スクリーンリーダー利用時は読み上げ完了まで進行を遅らせる代替フローを将来検討（v1.1 ではベスト努力）
- `prefers-reduced-motion: reduce` 時はフェードを即時化（200ms → 0）

---

### 1.6 削除・非推奨化される v1 規範

| v1 規範 | v1.1 での扱い |
|---|---|
| `GaborWithMask` コンポーネント | **廃止**。OPT-12 でマスク提示が消滅したため、本コンポーネントは v1.1 では使わない |
| `motion.duration.gameMaskInterval`（200ms） | **未使用化**（トークンは残すが参照しない） |
| `PeripheralLayout.showMask` / `presentationDurationMs` / `onTimeoutToMask` | **G-03 改修で削除**。G-03 は固視点 + 周囲 8 ガボールを 60 秒同時提示する型に変わる |
| 「Game 1 / 2 / 3」呼称 | **「G-01〜G-13」に統一**。UI 表示は「G-01 変化察知」「G-04 コントラスト弁別」など、ID + 日本語名併記 |
| ホーム CTA「3 分コースを始める」 | **「全ゲーム連続プレイ（約 13 分）」に変更**。N 動的計算 |
| 距離リマインドの「準備ができました」ボタン | **廃止** |
| 結果サマリの 1 試行内ループ表記（「N / M 試行」） | **廃止**。1 セッション 1 ゲーム 1 試行になったため |

---

### 1.7 タイポグラフィ・コントラスト規範（v1 から継承、強化）

- v1 の §2 タイポスケールはそのまま継承
- **`font.caption`（20px）の使用範囲をさらに限定**：v1 では「設定画面の subtitle」「グラフ凡例」などに使用していたが、v1.1 では 13 ゲーム連続コースの結果サマリ・進捗グラフタブ・バッジ一覧の主要文言は全て **24px 以上** にする。caption は「バッジ獲得日付」「データセット注釈」「グラフの軸補助ラベル」など本当の補助情報のみ
- v1 の §1.4 コントラスト規範（テキスト 7:1、装飾用途は AA 6.40:1 まで許容）を継続

### 1.8 v1.1.1 で追加するカラートークン（Sprint 20）

下記 4 トークンを `theme/tokens` に追加する（v1 / v1.1 のいずれにも未定義）。

| トークン | ライト値 | ダーク値 | 用途 |
|---|---|---|---|
| `color.selection.subtle` | `rgba(60, 64, 72, 0.55)` (#3C4048 55%) | `rgba(220, 224, 232, 0.55)` (#DCE0E8 55%) | F-07 改訂後の選択枠（線幅 2px、選択中）。背景 #808080 上で 3:1 以上のコントラスト確保 |
| `color.selection.subtle.idle` | `rgba(60, 64, 72, 0.30)` | `rgba(220, 224, 232, 0.30)` | 未選択時の枠（線幅 1px）。視覚的にセル境界を示す程度 |
| `color.semantic.successFg` | `#1F7A3A`（既存 success の暗い派生） | `#54C77A`（ダーク用） | GE-RESULT の ◯ アイコン本体色。背景白 80% 半透明上で 7:1 |
| `color.semantic.dangerFg` | `#A12727`（既存 error の暗い派生） | `#F4807F`（ダーク用） | GE-RESULT の ✕ アイコン本体色。同上 7:1 |
| `color.overlay.resultBg` | `rgba(255, 255, 255, 0.82)` | `rgba(20, 24, 32, 0.82)` | ◯ / ✕ アイコン下に敷く半透明背景円（パッチ縞模様を完全には隠さない） |

> 既存 `palette.semantic.success` / `palette.semantic.error`（v1 から継承）は装飾矢印などに使う色で、ガボール背景 #808080 上で 7:1 を保証していない。**◯ / ✕ アイコンは前面に半透明背景円を敷くことで、パッチ縞の上でも 7:1 を確保する**。これにより v1.1.1 のオーバーレイは AA / AAA 適合する。
> 黄色系トークン `color.highlight.correct` は v1.1 まで F-07 選択枠に使われていたが、v1.1.1 では選択枠用途では使わない（ResultSummaryV11 の独立画面が廃止されたため、結果ハイライトでも用途消滅）。トークン自体は将来用途のため残す（削除しない）。

---

## 2. v1.1 新規コンポーネント概要（詳細は components.md）

| ID | コンポーネント名 | 役割 |
|---|---|---|
| AC-1 | `AnswerChoiceGroup` | 13 ゲーム共通の選択肢ボタン群（2 / 4 / 8 / 9-25 / 10 択を統一 API で扱う） |
| AC-2 | `ImageChoiceCell` | グリッド型ゲーム（G-01、G-07、G-10）でパッチ自体が選択肢になるセル |
| MC-1 | `MetricCard` | F-10 結果サマリの「今回の閾値」「前回比」2 列カード（v1.1.1 で結果サマリ独立画面が撤去 + ResultOverlay からメトリクスバーも撤去されたため、新規用途なし。F-11 進捗グラフ等で再利用される可能性のため定義は残す） |
| RS-1 | `ResultSummaryV11` | 13 ゲーム共通の結果サマリ画面（v1 ResultSummary を統一） |
| GD-1 | `GameStatusBarV11` | 試行数表記廃止版（残り秒 + × のみ） |
| HM-1 | `HomeHeroCTA` | F-04 新ホームのプライマリ CTA カード |
| TB-1 | `ProgressGraphTabs` | F-11 ワイドスコア / ゲーム別タブ |
| TB-2 | `GameSubTabsScroll` | ゲーム別タブの中で 13 ゲームを横スクロール／グリッド表示 |
| DR-2 | `DistanceReminderV11` | 3 秒カウントダウン版（v1 DistanceReminder を置き換え） |
| RZ-1 | `DataResetNotice` | F-17 起動時データリセット通知（1 度のみ） |
| GS-1 | `GamePlaySurface` | 13 ゲーム共通の「上部 + 注視領域 + 下部選択肢」レイアウトコンテナ（OPT-12 骨格を強制） |
| GE-1〜GE-13 | 各ゲーム固有の刺激描画コンポーネント（G-01〜G-13） | 注視領域の中身（ガボール配置・グリッド等） |

詳細は `components.md` を参照。

---

## 3. F-18 `releaseEnabled` 動的除外の設計上の保証

仕様 F-18 は「`releaseEnabled=false` のゲームをホーム / コース / グラフ / バッジから動的除外する」を要求します。設計上、以下の規範で**矛盾発生を抑止**します。

### 3.1 単一ソース：gameRegistry
- 13 ゲームの定義（ID / 表示名 / 系統 / `releaseEnabled` / `order`）は `gameRegistry` に集約
- 全画面・全コンポーネントは `gameRegistry.filter(g => g.releaseEnabled)` を経由してゲーム一覧を取得する
- **直接ゲーム ID 配列をハードコードしない**（screens.md でも `[G-01, G-02, ...]` と書かず、「enabled な全ゲーム」と表現する）

### 3.2 反映ルール

| 画面 / 機能 | 反映ルール |
|---|---|
| ホーム CTA「全ゲーム連続プレイ（約 N 分）」 | N = enabled ゲーム数。CTA ラベルが動的計算 |
| 全ゲーム一覧（単体プレイ） | enabled ゲームのみ表示。Card 並びは `order` 昇順 |
| 全ゲーム連続コース | enabled ゲームのみが対象。順序は `order` 昇順 or 日付シードランダム |
| 進捗グラフ「ワイドスコア」 | enabled ゲームの正規化スコア平均で算出 |
| 進捗グラフ「ゲーム別」子タブ | enabled ゲームのみタブとして表示 |
| バッジ一覧（B-01〜B-13） | バッジ自体は全 13 種を一覧表示するが、獲得条件評価は enabled ゲーム集合で再評価。disabled に依存するバッジは「獲得条件 × 該当ゲーム除外で評価」と注記表示（後述） |
| ゲーム単体起動の URL ／ DeepLink | disabled ゲームへのアクセスは「現在このゲームは公開対象外です」を表示してホームに戻す |

### 3.3 バッジの動的除外文言

`releaseEnabled=false` のゲームに依存するバッジ（B-06、B-07、B-08、B-09、B-10、B-13）は、disabled ゲームを除外した集合で評価し、未獲得時のヒントには
> 「現在公開対象の N ゲーム全てで条件を満たすと獲得」

のように動的な N を表示します。

### 3.4 設計上のチェック手順
- Designer は 13 ゲーム分のモックを作成するが、**画面のレイアウトは「ゲーム数が 7〜13 のいずれでも崩れない」前提**で設計
- 横スクロールタブは折り返しでなくスクロール可。グリッドは 2〜4 列で自動列数調整
- 結果サマリ「次へ」ボタン押下時、コースの最終ゲームかどうかは `currentIndex === enabledGames.length - 1` で判定（13 固定でない）

---

## 4. レスポンシブ規範（v1 と同じ。再掲）

- スマホ縦：360 / 375 / 390px で動作確認
- タブレット縦・小型 PC：768px
- PC 横：1280px
- ガボール領域は `min(viewport.short_edge × 0.6, 480px)` を上限

---

## 5. アクセシビリティ規範（v1 と同じ。再掲・強化）

- 全インタラクティブ要素 48pt 以上、ボタン 56pt 以上（OPT-2）
- focus-visible 3px outline + 2px offset（NF-9）
- 色のみで状態を伝えない（NF-12）。13 ゲーム全てグレースケールのため色覚異常配慮は v1 同様不要
- prefers-reduced-motion 尊重（NF-13）
- Skip link（NF-14）：ホーム上部に「メインコンテンツへスキップ」
- aria-checked / aria-pressed で選択状態を明示（NF-15）
- スクリーンリーダー：13 ゲーム全てに「ゲーム説明」`aria-describedby` を必須

---

## 6. i18n キー命名規約（v1 から継承）

v1.1 で追加・改訂される文言は以下のキー命名規則に従います。

```
# F-04 ホーム
home.cta.fullCoursePlay = "全ゲーム連続プレイ（約 {n} 分）"
home.link.singleGameMode = "単体プレイ（{n} ゲームから）"
home.completed = "本日完了"

# F-10 結果サマリ統一
result.title = "{gameName} の結果"
result.correctIs = "正解は「{answer}」"
result.yourAnswer = "あなたの回答「{answer}」"
result.threshold.label = "今回の閾値"
result.diff.label = "前回比"
result.diff.improved = "改善"
result.diff.flat = "同等"
result.diff.worsened = "やや低下"
result.firstMeasurement = "初回測定"

# F-16 距離リマインド改訂
distanceReminder.headline = "画面から {n}cm 離れてください"
distanceReminder.autoStart = "{n} 秒後に自動で開始"
distanceReminder.oneEye = "片目を覆ってください（任意）"

# F-17 起動時データリセット
dataReset.title = "最新版へのアップデート"
dataReset.body = "過去のデータをリセットしました"
dataReset.cta = "OK"

# 13 ゲームの説明・選択肢
game.G-01.name = "変化察知"
game.G-01.description = "わずかに角度が変化していくパッチを見つけてください"
game.G-04.name = "コントラスト弁別"
game.G-04.choice.left = "左が濃い"
game.G-04.choice.right = "右が濃い"
... （以下、各ゲーム）

# F-11 進捗グラフ
progress.tab.wideScore = "ワイドスコア"
progress.tab.gameByGame = "ゲーム別"
progress.empty.lessThan7Days = "もう少しデータが集まると傾向が見えます"

# F-13 バッジ
badge.B-01.name = "はじめの一歩"
badge.B-01.condition.locked = "初回フルコースを完了すると獲得"
... （以下、B-02〜B-13）

# F-18 公開対象外
game.disabled.notice = "現在このゲームは公開対象外です"
```

---

## 7. システムバージョン

- v1.1（凍結対象）
- 後続変更は Designer amendment モードで実施（オーケストレーター承認必須）
